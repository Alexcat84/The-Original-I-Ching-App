/**
 * Post-checkout URL path. Set your Web Billing success / return URL to:
 * `${NEXT_PUBLIC_APP_URL}${CHECKOUT_SUCCESS_PATH}` (e.g. https://example.com/checkout/success)
 */
export const CHECKOUT_SUCCESS_PATH = "/checkout/success" as const;
