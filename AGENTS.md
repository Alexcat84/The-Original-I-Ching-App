## Learned User Preferences
- Prefiere que el agente continúe desde el estado actual del proyecto sin reanalizar desde cero cuando hay contexto previo.
- Suele pedir ejecución directa ("procede", "hazlo") en lugar de planes extensos.
- Solicita con frecuencia `commit` y `push` tras cerrar un bloque funcional para validarlo en despliegue (habitualmente vía rama `staging`); no promueve merge `staging`→`main` hasta validar estabilidad en staging, incluidas pruebas Warp/PostgREST en staging antes de promover. Orden preferido al cerrar trabajo en una rama feature: **commit + push a la rama feature PRIMERO, luego `merge --no-ff` a `staging` + push, y documentar el cambio en `docs/auditorias/`** (mantener el doc de auditoría del feature al día por partes). Si el working tree está en `staging`, stashear, actualizar la feature con `staging` y reaplicar para evitar conflictos. En cierres de fidelidad PDF o mutaciones en rama feature, **no merge a `staging` hasta pedido explícito**. Con features paralelas (p. ej. `feature/wilhelm-de-dataset` mientras `staging` lleva otro bloque), smoke en **Vercel Preview** de la feature antes de merge; overlay inglés en `main`/prod con traductor Wilhelm es esperado hasta promover la feature — no atribuir a fallo local sin confirmar rama/URL desplegada. En releases Play/Android pide explícitamente merge `staging`→`main` seguido de EAS `production` AAB en la misma sesión. Notas «What's new» vía [`docs/00000000-OPS-PLAY-01-play-store-changelog.md`](docs/00000000-OPS-PLAY-01-play-store-changelog.md): **no repetir** bullets de releases anteriores; cada versión solo lo nuevo; entregar bloque bulk-paste completo con tags Play Console `<en-US>`…`<zh-CN>` (11 locales, orden fijo); sin jerga interna (AU, Monte Carlo, scraping).
- Prefiere separar auditoría/investigación de remediación: en fase de auditoría pide reporte detallado y pruebas reproducibles sin aplicar fixes hasta confirmar alcance; entrega `.md` en `docs/auditorias/` según el dominio. Con riesgo legal copyright documentado (Baynes inglés 1950), **corregir copy público** FAQ/notes/`licenseNote` de inmediato antes de otro trabajo funcional; no solicitar licencia Princeton — ruta producto = runtime Wilhelm **alemán 1924 PD**. En investigación huesos de oráculo/Keightley: **documentar primero** — no tocar copy, motor ni FAQ hasta pedido explícito; priorizar contexto arqueológico completo sobre gold o comparativas prematuras. Al crear o renombrar docs en `docs/` o scripts test/gate: registrar en `registry.json` + `INDEX.md` (WF-DOC-02/WF-QA-01), cabecera QA con `area`, y pasar `npm run verify:qa-registry` antes de cerrar — códigos test `{TIPO}-{FAMILIA}-{NNN}` sin `YYYYMMDD`; docs `{YYYYMMDD}-{TIPO}-{FAMILIA}-{NN}`. Al auditar con repos de skills externos (p. ej. Claude-BugHunter), audita **iching-app** aplicando esas skills, no el repo fuente de skills. En fidelidad de datasets `@iching-oracle/iching-data` (`npm run verify:hexagram-fidelity` → `reports/hexagram-fidelity-*`), valida hallazgos con auditoría externa antes de remediar; prefiere re-ingesta desde gold sobre parches manuales; exige validar parser gold antes de confiar en `% match` del harness — el gate EPUB parser↔bundle es circular y no sustituye revisión humana. En W/L EPUB actúa como **juez definitivo** con capturas del libro físico cuando comparativos o gates no resuelven la duda; prefiere verificación manual campo a campo (Google Sheets vertical `campo`/`contenido_epub`, sin símbolos decorativos en export / visor en orden del libro) sobre confiar solo en `% match` automatizado. En maestro Wilhelm DE desde zeno.org (Gemeinfrei / PLAN-DAT-W-03), exige **100% de extracción de campos presentes en la fuente** — no equivalencia literal contra Baynes EN; pares estructurales EN↔DE y MT DE→EN solo para alineación/comparador HTML (**AUD-DAT-W-04**, diagnóstico, no gate fidelidad). En AU Wilhelm DE Ten Wings (Anna OCR + JPG): **no promover** sandbox ni `comments-merged` hasta `contenido_pdf` + `au_estado` ∈ {cerrado, vacio_en_libro} por fila — heurística reconcile nunca cierra disputas sola; contrato `tools/manual-gold/wilhelm-de-comments-au/au-contract.json`. **No lee alemán:** al mapear secciones del libro explicar marcadores DE (`Erstes Buch`, `Zweites Buch`, `Drittes Buch`, `Kommentar zur Entscheidung`, etc.); mapea inicios hex Erstes Buch por headers numerados en JPG — Ten Wings no siguen esa numeración (alas repartidas entre Zweites bloques y Drittes por hex). Antes de switch runtime Wilhelm DE: entregar **estado actual** documentado antes del plan de conmutación; al cerrar switch retcon AU Baynes como referencia nunca canónica y marcar Pantheon **Obsoleto** en `/audits`. Antes de re-sync masivo desde EPUB, exige comparativa contra el dataset runtime actual — no reemplazar wholesale lo ya validado. Antes de smoke post-fix, contrastar con fixes previos (changelog/commits) para no reintroducir regresiones ya cerradas (p. ej. logout OAuth WebView). En QA/benchmarks de interpretación (`npm run qa:mutation-output`, mutaciones) y lectura base (`npm run qa:reading-quality` → `scripts/reading-quality-qa.mjs`, ruta NO_CHANGING, `--random N` smoke), ejecuta pruebas que consumen tokens cuando validan valor de producto; exige **trazabilidad explícita del modelo por respuesta**. En reading-quality QA, comparar blockquotes juicio/imagen **letra a letra** contra bundle local `packages/iching-data/src/generated/hexagrams.{wilhelm,legge}.json` — no git, no EPUB/PDF externo, no `% match` heurístico solo; rechazar términos vagos sin diff carácter a carácter. **Reintentos de calidad LLM que dupliquen llamadas Anthropic no son viables** (tokens consumibles); la cadena fallback sí debe garantizar respuesta al usuario — ver `20260622-AUD-MUT-05-retry-fallback-economics.md`. En estabilidad Supabase exige auditoría del agente Supabase **1:1 contra plan cerrado**, gates de integridad de datos (no solo smoke Warp/PostgREST), **monitoreo en vivo de logs API** (`get_logs` con `service:api`, no `postgres`) durante smoke prod, correlación causal Axiom+Vercel antes de cerrar PASS, y validación con logs antes de cerrar fases, migraciones destructivas o builds; en smoke **no atribuir al tester un logout manual** — `POST /auth/v1/logout` suele ser `signOut()` automático tras 401/Warp en bootstrap/chats. En gates de seguridad móvil (Play Integrity) exige cadena Axiom correlacionada (`traceId`) **y** alertas Sentry en `integrity_check_failed`/fallos del bridge — no basta con `log.warn`; si el error persiste post-fix, análisis forense antes de remediar. Para **production readiness** pide auditorías con skills/plugins aplicables del workspace, lanzando agentes en paralelo por dominio cuando aporte cobertura.
- Tras incidentes P0 de pérdida de datos exige runbooks permanentes en `docs/runbooks/` y cuestiona si el impacto con usuarios reales en producción fue previsto antes de aplicar migraciones destructivas.
- Valida planes de implementación antes de ejecutar cambios; pide confirmación explícita de alcance y efectos colaterales. **Antes de proponer eliminar, deprecar o sustituir código, módulos o archivos** (p. ej. ante warnings de build): verificar uso real con grep/import chain y rutas de producción; **nunca** sugerir borrado como atajo sin esa verificación. Solo después presentar **opciones X o Y** (mitigar in-place, configurar, refactor acotado) con trade-offs explícitos — no «elimínalo» si puede romper overlays, consultas, PDF, billing u otras rutas críticas. En upgrades Expo/SDK bloqueantes de Play exige gate go/no-go temprano en dependencias nativas críticas (`expo-app-integrity` con `prebuild --clean` sin hacks) y prefiere dos releases cuando variables son interdependientes (p. ej. SDK 53 + 16 KB antes que edge-to-edge). **No tocar billing** (Stripe web, Play Store/RevenueCat nativo, webhooks, product IDs) sin aprobación explícita — solo copy i18n en diálogos permitido sin cambiar lógica de compra. Al sincronizar docs de desarrollo (README, canvas, CLAUDE.md), no tocar docs internos de producto (`guia/`, notas, FAQ) salvo pedido explícito.
- Suele validar móvil con APK local (Gradle, p. ej. assembleDebug) y reserva EAS o subidas a Play para cuando lo pide expresamente; fixes del bridge auth WebView (`auth_signout`, `INJECTED_JS`) exigen **deploy web + APK** — solo Vercel o solo APK deja regresión OAuth.
- Valora respuestas y UX homogéneas en el idioma del usuario, evitando mezcla ES/EN; prefiere **español latinoamericano**, no español de España, en respuestas y copy. Exige i18n estandarizado sin strings hardcodeados en UI, con inglés (EN) como fuente de verdad y claves en `@iching-oracle/i18n`; vigila regresiones en web y WebView/APK. Etiquetas regla mutación Zhu Xi (`ZX_*` en `iching-mutation-ui.ts`): sin sufijo redundante «(Zhu Xi)» — la fila «Lectura de líneas» ya identifica el sistema; alinear copy con reglas Huang equivalentes. Reglas mutación en resumen tirada: **etiqueta corta** i18n alineada al traductor y sistema Huang/Zhu Xi seleccionado; texto literal `mutationRuleBookText` + traducción completa solo en `/mutation-explorer`. En re-aceptación de términos/privacidad, encabezado de actualización si ya aceptó antes; en `/auth/complete-legal` sin aceptación previa, encabezado neutro (no el de creación de cuenta). En docs de producto (`guia/`, `notes`, FAQ) evitar em-dash/en-dash (señal de texto IA). En copy orientado al usuario (guías, marketing, onboarding): **no publicar precios**; varas de milenrama **no** describir como «más lento» en automático (solo el manual es pausado/táctil); consulta en cualquier idioma con respuesta en el **idioma de la UI**.
- Prioriza calidad visual y experiencia cuidada por encima de entregas mínimas, y prefiere implementaciones reales sobre mocks cuando ya existen credenciales y presupuesto para proveedores; export PDF de chat es funcionalidad principal — no eliminar ni bloquear por mitigaciones (OOM, etc.) sin alternativa equivalente.
- Prefiere commits separados por etapas en refactors amplios (p. ej. i18n por fases); cuando se sustituye WebView por UI nativa, exige paridad funcional completa (auth, 2FA, chats, docs, compras, etc.) y rechaza entregas a medias.
- Ante recomendaciones de índices del dashboard Supabase, exige informe de viabilidad antes de DDL; si las rutas calientes ya usan índices compuestos existentes, prefiere no añadir duplicados y limitar follow-up a VACUUM FULL (TOAST) y ticket Support vía plantilla `docs/runbooks/00000000-RUN-SUP-02-supabase-scalability.md` §8 (objetivo: pool PostgREST ~10, no compra de compute Medium).
- Página pública `/audits` (WF-DOC-03): solo **verificaciones de fidelidad de datos**, nunca releases de producto; **4 secciones** en orden fijo — oráculo → comentarios clásicos (W/L) → métodos de tirada → mutación; **14 entradas** (6 oráculo + 2 biblioteca + 4 tirada monedas/varas + 2 mutación); **una entrada por método** en `divination-method` (monedas vs varas, cada uno con AU inicial + AU book-primary); AU inicial DIV-01 = Monte Carlo + tablas Nielsen/Rutt, **no** Apéndice Wilhelm; AU publicada = Apéndice I (1950); leer `docs/auditorias/` aplicable antes del copy público; conciso y fuente nombrada (si no está documentada, citar referencia respetada del dominio); campo Estado = **solo** «Vigente a la fecha.» u «Obsoleto.» (con punto, sin texto suplementario ni referencias a auditorías futuras); `currentStatusNote` vacío (UI muestra solo `statusLabel`); obsoletos en rojo en UI; fechas verdes `current`/`permanent`, grises `superseded`; conectores del timeline hasta el último campo con curvas suaves (no a 90°), legibles en claro y oscuro (`globals.css` `.audit-timeline`, 1.5px solid); enfatizar **Final: X/Y 100%**; copy sin «scraping/scrape»; `/notes`/FAQ solo enlace.

## Learned Workspace Facts
- `The Original I Ching App` combina consultas I Ching con generación de imágenes; privacidad central: chats e imágenes privados por usuario autenticado, persistencia server-side/DB (no almacenamiento local efímero para datos de usuario).
- Motor huesos de oráculo (`oracle-bones-engine`): 4 veredictos, pesos y patrones T/X/Y = diseño producto desde initial commit; Keightley 1978 es la única fuente académica seria citada — PDF en `tools/source-pdfs/`, gold procedural vía `npm run extract:gold:keightley-procedural`; ancla ritual Shang (carga ±, anti-silencio) pero no taxonomía 4×4 ni pesos; matriz respaldo producto vs fuentes en `AUD-DIV-04`; FAQ/copy «faithful to Shang» = sobreafirmación documentada (`AUD-DIV-03`); `/audits` excluye huesos hasta AU book-primary; trazabilidad en `BRIEF-DIV-04`/`05` + `AUD-DIV-04`; `/notes` orden I Ching → huesos → IA → bibliografía APA; sección huesos nunca tuvo URLs (Wikipedia histórica may 2026, 9 enlaces, retirada `fd1ce52`).
- El flujo de imágenes incluye overlays de texto chino y marca de agua; los prompts pasan por compactación/recorte (`compactPrompt`/`buildImagePrompt`) con restricciones críticas (p. ej. negativas / sin caligrafía decorativa) al inicio del string; la categoría `family_home` en `packages/image-engine` puede sesgar hacia tropos de interior/patio. Overlay PNG (`buildSumiHexagramOverlaySvgDataUrl` + `embedCjkFontInOverlaySvg`): dual font-stack — `overlay-title-zh` embebe Noto Serif TC; `overlay-title-en` embebe Noto Serif Latin-Extended (nombres Legge con ă/Ž/etc.); gate `npm run verify:overlay-glyphs`. Segunda fila overlay = `castResult.primaryHexagram.name` vía `@iching-oracle/iching-data` **dist** (sin traducción aparte en renderer); `packages/iching-data/dist/` gitignored — web **`predev`/`prebuild`** reconstruye `iching-data` (`copy-generated.mjs`); dist stale muestra nombres EN local. Trigramas (`trigrams.json`) solo Biblioteca (no overlay); labels DE canónicos + `trigramIdFromWilhelmLabel` con aliases Baynes/OCR (`scripts/lib/wilhelm-trigram-labels.mjs`); picker UI sigue 漢字+pinyin; hanzi/glyphs overlay sin cambio con Wilhelm DE.
- El cliente Android (`apps/mobile`) carga la experiencia web en un `WebView`; la UI y el login dependen del despliegue remoto, no solo del APK. **Stack Release A (SDK 53, 4.2.0/vc60):** Expo ^53, RN 0.79.6, React 19 mobile (`newArchEnabled: false`, Sentry 6); web sigue React 18 vía su `package.json` — reconciliar `overrides` del root tras upgrades mobile. Builds Play/prod usan `apps/mobile/.env` con bloque **PRODUCTION** activo (`EXPO_PUBLIC_API_URL=https://theoriginaliching.com`, Supabase prod, `EXPO_PUBLIC_APP_ENV=production`) más variables duplicadas en EAS; `extra.apiUrl` (embebido en release) es la fuente de verdad vía `Constants.expoConfig`. **Play galería save-only (vc48+):** `blockedPermissions` READ_MEDIA_* en `app.config.js` + `MediaLibrary.usePermissions({ writeOnly: true })` — solo `saveToLibraryAsync`, sin lectura de galería; PDFs vía `Sharing.shareAsync` (no MediaLibrary). **EAS gotchas SDK 53:** `@sentry/react-native` 6 requiere dep `promise` (RN 0.79); `expo-splash-screen` ~0.30 exige `splash.image`/`expo-splash-screen` plugin con `image` o Gradle falla `splashscreen_logo not found`; `expo-app-integrity` puede necesitar postinstall `fix-expo-app-integrity-gradle.js` (AGP 8). **Release B (edge-to-edge):** plugin `react-native-edge-to-edge` en `app.config.js`, `SystemBars` en `_layout.tsx`, insets `--rn-safe-area-inset-top/bottom` inyectados al WebView (sin `RNStatusBar.setBackgroundColor`). Tras `prebuild --clean` local en Windows reaplicar fix `glob` en `@expo/config-plugins` (CLAUDE.md). `INJECTED_JS` puede sobrescribir estilos remotos con `!important`; safe area RN vía `--rn-safe-area-inset-top/bottom` inyectados desde `useSafeAreaInsets` (ver audit `docs/auditorias/20260618-AUD-MOB-UI-01-webview-bottom-gap.md`): `.chat-surface` `margin-bottom: 0`; composer `calc(0.42rem + var(--rn-safe-area-inset-bottom))`; drawer spacer `calc(0.75rem + var(--rn-safe-area-inset-bottom))` en `INJECTED_JS` — no usar `max(18px,…)` ni `clamp(18px,…)` en contenedores padre. Mitigación OOM WebView: descarga chunked; PDF móvil con imágenes JPEG ~72% max ~960px. Cache SQLite 3 capas + API remota meta/content sin TOAST; `FETCH_TIMEOUT` 25s. **Play Integrity:** `useIntegrityCheck` + `attestKey`; nonce Upstash; telemetría Axiom+Sentry (`integrity-sentry.ts`). Validar web + APK en cambios Play/locale. Arqueología: `docs/auditorias/00000000-AUD-MOB-HYD-02-sqlite-chat-hydration.md`, `docs/auditorias/00000000-AUD-MOB-HYD-01-chat-thread-hydration.md`.
- Login Phase 3: `GET /api/account/bootstrap` concentra billing/perfil/legal/resumen de sesiones (reemplaza `me` + `chats?summary=1`). Race remediado: `bootstrapCompletedForRef` solo tras respuesta exitosa (`setTierReady(true)`); marcar completado antes del fetch + cancelación React dejaba skeleton eterno de pack/tokens pese a datos OK en DB; `bootstrapInFlightRef` evita fetches duplicados. Auth retry: `fetchWithAuthResilience` + `shouldSignOutAfterAuthResilience` en bootstrap/chats; `fetchWithSessionRetry` en account refresh; señal nativa `bootstrap_complete` coordina `syncChats`. Bridge nativo baseline `b111d7b`: debounce 1200ms antes de `auth_signout`, sin redirect `/login` en sign-outs transitorios (regresión reintrodució kick OAuth en Chrome externo). Consult/SSE aún pueden `signOut` en 401 — riesgo residual bajo Warp. Cold open con JWT cacheado puede kick a OAuth si bootstrap/chats caen en burst Warp antes de estabilizar retries. Onboarding tour lifetime: `users.tour_v1_completed_at` (051) vía `POST /api/account/tour-complete`; bootstrap expone `tour_v1_completed`; `localStorage` (`iching_tour_v1`) es caché — botón ⓘ replay manual. Biblioteca hexagramas: textos vía `/api/library/[number]` Bearer Seeker+; rutas legacy `/api/hexagram*` eliminadas; UI comentarios colapsables (+/− cian del tema) en `/library/[number]` promovida a `main` 2026-06-24 — audit `20260624-PLAN-LIB-03-library-ribbon-ui-fix.md` cerrada. Verificador reglas `/mutation-explorer` (castIndex **1–4096**): nav junto a Biblioteca; modo A `?cid=` desde resumen (autenticado, consulta propia); modo B manual **Seeker+** (free ve enlace deshabilitado).
- RevenueCat Android: `Purchases.logIn` es async tras cold start — compra antes de completar puede enviar `$RCAnonymousID` al webhook (el route lo ignora con 200). `grant_tokens` (migración 044) acumula tokens siempre y solo bloquea `last_pack = 'free'`; downgrade de tier por pack más barato es válido.
- Changelog en raíz (`CHANGELOG.md`, `npm run changelog:generate` / `changelog:update`); checklist pre-release en `scripts/pre-release-checklist.sh`. Ritual timing en `apps/web/src/lib/iching-ritual-timing.ts` + `NEXT_PUBLIC_ICHING_RITUAL_*` (contrastar con SSE real de `/api/consult`, no solo timers UI). Mapa arquitectura: [`ARCHITECTURE_SYSTEM.canvas.tsx`](ARCHITECTURE_SYSTEM.canvas.tsx). Monorepo vía `npm` (npm@10), no `pnpm`. Codificación docs/QA: `docs/auditorias/registry.json`, `docs/qa/registry.json`, índices `INDEX.md`, workflows WF-DOC-02/WF-QA-01; gates `npm run verify:qa-registry`, `inject:qa-headers`; exenciones pipeline en `docs/qa/exempt-paths.json`; sin scripts sueltos en raíz — one-shots → `scripts/archive/`.
- i18n centralizado en `@iching-oracle/i18n` (`packages/i18n`) con `Record<AppLocale, …>` por módulo; referencia EN (`DEFAULT_LOCALE` en `locales.ts`); no usa `next-intl` ni `apps/web/messages/*.json`. Página `/audits` (`audits-page-ui.ts`, WF-DOC-03): timeline por 4 categorías; Zhou Yi en dos fases; `/notes` y FAQ enlazan. Guías: [`00000000-WF-I18N-01`](docs/workflows/00000000-WF-I18N-01-i18n-guide.md), [`00000000-WF-I18N-02`](docs/workflows/00000000-WF-I18N-02-i18n-standardization.md).
- Estabilidad Supabase (`docs/auditorias/`): plan por fases completado hasta **069** — `consultation_content` es única fuente de verdad (lectura `get_session_content_safe`, escritura `persist_consultation_with_content`); sin columnas TOAST legacy ni trigger `sync_consultation_content` en `consultations`. Compute prod **Small** (add-on ~$10 sobre Pro ~$25); ticket Support pide aumento pool PostgREST **~10**, no tier Medium (el «30» del resumen AUD-SUP-02 es incorrecto). Pool fijo (Pro no lo eleva): cuello = amplificación requests paralelos (bootstrap + meta + content). Prod: `statement_timeout` **120s**, `max_connections` **90**. Warp/57014 reproducibles en prod con un solo usuario bajo burst; patrón **consultations meta SELECT lento + lock_io/checkpoint + burst conexiones**. Warp cada ~60s puede continuar **sin APK ni pestaña usuario** — `/api/health` en Vercel (`users?select=id&limit=1`, user-agent `node`), pg_cron prewarm 15min, pool saturado; no prueba bug cliente. Monitoreo MCP: `get_logs` **`service:api`** (ventana **~24h**; 14d requiere Logs Explorer dashboard); inventario migraciones vía `verify_migrations.sql`/`execute_sql`, no `list_migrations` MCP. Telemetría: `supabase-telemetry.ts` + Axiom **`iching-app-main`** / **`iching-app-staging`**. **P0 2026-06-07:** 066 sin 068 prohibido. TOAST huérfano post-069: `VACUUM FULL consultations`. Runbooks: `MIGRATION_DATA_INTEGRITY`, `RECOVER_CONSULTATION_CONTENT_PITR`, `SUPABASE_POST_RESTART`, `SUPABASE_SCALABILITY` (§8 plantilla ticket). MCP prod por defecto; staging (`pjbjpdpgpzwgrellvsor`) requiere credenciales explícitas — verificar vía PostgREST + Axiom `iching-app-staging`, no MCP. Crashes OOM WebView Android (p. ej. Sentry) no son verificables vía Supabase.
- Selector lectura de líneas (Huang default / Zhu Xi) + idioma consulta: orden panel **Traductor → Lectura de líneas → Método → Ejecución**; barra de estado y resumen de tirada reflejan la selección (no hardcodear Zhu Xi). Persistencia `consultations.line_reading_system` (074); aplicar migración **antes** del deploy (cascada lectura `session-store` no degrada columna ausente). Expuesto en `/api/consult`, SSE `final_ready`, PDF y tagline. Idioma vía `detect-input-language.ts` compartido cliente/servidor. Reglas Zhu Xi verificadas vs Adler (Yixue Qimeng ch. IV): gold `tools/output/fidelity-gold/zhuxi-adler-mutation-rules-gold.json`; Qian/Kun con `readBothJudgments` + 用九/用六. Audit: `20260620-AUD-LRS-01-zhuxi-line-reading-selector.md`.
- Fase 2 reglas mutación I Ching (`backend/claude`, audit `docs/auditorias/20260615-AUD-MUT-02-prompt-mutation-gates.md`): prompt con `INTERPRETED_LINES`/`OMITTED_CHANGING_POSITIONS`; gates bloqueantes H1/H3/H5 + retry H2 (streaming incluido) — H2 hoy reintenta calidad con llamadas Anthropic extra (política producto en revisión; ver `20260622-AUD-MUT-05-retry-fallback-economics.md`); H7 (`interpretation-judgment-image-gate.ts`) es **warn-only** + Sentry (`judgment_image_verbatim_drift`), sin reintento; `InterpretationQualityError` activa fallback OpenRouter→Groq (Opción B, sin refund automático) para que el usuario siempre reciba respuesta. MUT-08: reglas SSoT en bundles `mutation-rules.{huang,zhuxi}.json`; prompt Claude recibe `mutationRuleBookText` EN verbatim; resumen UI usa `getIchingMutationRuleSummaryLabel` (corta); explorer muestra bookText + traducción i18n. QA: `scripts/mutation-output-qa.mjs` (`npm run qa:mutation-output`) — matriz fixtures × traductores × modelos vía `buildAnthropicUserPayloadForCast` + `buildAnthropicInterpretationParams` (paridad prod depth=1 primera consulta); cada fila JSON incluye `model`, `fixtureId`, `translator` en `reports/mutation-qa-*.json`.
- Fidelidad traductores `@iching-oracle/iching-data`: harness `npm run verify:hexagram-fidelity` → `reports/hexagram-fidelity-*`; resumen público en `00000000-RPT-DAT-FID-00-data-integrity-summary.md`; audits en `docs/auditorias/`. **Gold producción book-primary:** Legge Oxford PDF **514/514**, Zhou Yi ctext.org **514/514** (permanente); Wilhelm en `main`/`staging` = **Baynes EN 1950** Pantheon **513/513** (copyright ~2046 USA, **no** dominio público) — EPUB/PDF en `tools/source-pdfs/` = gold QA interno; **Wilhelm DE 1924** Diederichs (PD USA 2020) promovido en `feature/wilhelm-de-dataset` como futuro runtime `translator:"wilhelm"` — blockquotes juicio/imagen **en alemán literal** (modelo Zhou Yi); fuente canónica citada **Diederichs 1924**, zeno.org solo extracción Gemeinfrei (**Erstes Buch** oráculo + **Zweites Buch** material; **no** publica Drittes Buch/Ten Wings). Escaneo JPG 585 páginas = **un solo volumen** Diederichs: Erstes p23–212, Zweites p213–314 (Schuo Gua/Da Dschuan bloques), Drittes p315–585 (Die Kommentare por hex). **Book-one runtime** = Zeno 514/514; **Ten Wings comments** = sandbox Anna OCR (`tools/datasets/wilhelm-de/comments/anna/`, passes 02/04) + AU JPG — `comments-merged` vacío hasta gate `validate:wilhelm-de-comments-au-gold`; promote solo vía `contenido_pdf` + `au_estado` (`au-contract.json`). Promoción maestro: extract zeno limpio + hanzi/`hex_font` desde Zhou Yi (`iching_zhouyi_translation.mjs`) → sustituye `wilhelm-de-64hex-merged.json` (OCR merged archivado); pipeline `extract:wilhelm-de-from-zeno` + `wilhelm-de-zeno-{parse,split}`, blank maestro `tools/datasets/wilhelm-de/book-one/wilhelm-de-64hex-blank.json`, gold TSV `tools/manual-gold/wilhelm-de-zeno-hex-*.tsv`, gate **zeno-content 514/514** + pares estructurales Baynes **1926/1926** (diagnóstico); comparador `export-wilhelm-de-baynes-comparison-viewer.mjs --de-maestro=zeno extract`; gate `verify:hexagram-fidelity:wilhelm-de` era circular vs OCR merged — **rebasar gold** post-promoción zeno; Baynes archivado solo triangulación (`iching_wilhelm_translation.baynes.mjs`); cierre switch `/audits`: entrada Baynes Pantheon → **Obsoleto**, nueva AU Wilhelm DE 1924 canónica; no merge a `main`/`staging` hasta smoke Preview explícito; OCR PDF/JPG como primario abandonado tras regresión juicios; datasets legacy Parma/sacred-texts confirmados incompletos/contaminados — no reutilizar como baseline. Gate EPUB `bundle==parse` es **circular** (no detecta defectos de parser p. ej. verso «The image of X.» filtrado). Maestros EPUB/TXT W/L (`tools/datasets/wilhelm/`, `tools/datasets/legge/`, `runtimeIngest: false`) en paralelo al runtime — 64hex, comentarios y apéndices en archivos separados; no promover a bundles producción hasta AU/cierre explícito; no normalizar ortografía maestro vs libro. Durante verificación/re-ingesta W/L no tocar bundles runtime de producción hasta cierre explícito. **Zhou Yi** = 经 clásico (`iching_zhouyi_translation.mjs`) **sin notas** — no mezclar Zhu Xi/Benyi ni TXT *Original Meaning* (inglés) en dataset Zhou Yi. **Reading-quality QA:** `validateInterpretationOutput` no gatea verbatim juicio/imagen (Wilhelm puede 64/64; Legge imagen puede omitir prefijos trigramáticos o paréntesis Legge pese a PASS estructural). Sin globalizar `Kien→Khien` si bundle/EPUB usa `Kien`.
