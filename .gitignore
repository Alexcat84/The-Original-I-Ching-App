node_modules
# Editor / agent local rules — never commit
.cursor
.claude
mcps
.turbo
dist
.next
out
build
*.tsbuildinfo
# Never commit environment files or templates (including .env.example).
.env
.env.*
*.log
.DS_Store
coverage
.expo
apps/mobile/.expo
apps/mobile/dist
.vercel
tools/output/
# Temporary / local artifacts — never commit
*.keystore.bak
*.jks.bak
tools/fallback-tools/_sim*.mjs
# Claude Code / agent runtime state — never commit
.agents/
.tmp/
skills-lock.json
# Temporary QA / audit artifacts (root junk + line-reading QA outputs) - never commit
mis_logs.json
reportsnpm-audit-raw.json
reports/lrs-qa-*
